extends layout

block content
  .container.flex-fill
    .row.align-items-center
      .col
        h1.big-text.mb-3 401
        h3
          | Non hai le credenziali per fare questa cosa che stai provando a fare.
          br
          | Ripensaci.
        if errorMessage
          h5.white.mt-4
            | Gaben ci tiene ad aggiungere:&nbsp;
            br
            | '#{errorMessage}'
